"use client";

import { useEffect, useState } from 'react';
import { useParams } from 'next/navigation';
import { AlertCircle, ArrowDownRight, CalendarDays, HeartPulse, Loader2, ShieldAlert } from 'lucide-react';

type Visit = { task_id: string; waktu: string; foto_bukti_url: string | null; catatan_kondisi: string | null; health_snapshot: { energi: number; mobilitas: number; mood: number; nafsu_makan: number; kualitas_tidur: number; cerita_hari_ini: string | null } | null };
type HistoryResponse = { lansia: { nama: string }; kunjungan: Visit[]; tren: { rata_rata_terakhir: number | null; perubahan: number | null; perlu_perhatian: boolean }; disclaimer: string };

export default function RiwayatRangkulPage() {
  const { id } = useParams<{ id: string }>();
  const [data, setData] = useState<HistoryResponse | null>(null);
  const [error, setError] = useState<string | null>(null);
  useEffect(() => {
    fetch(`/api/lansia/${id}/riwayat`).then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.message || 'Riwayat tidak dapat dimuat'); setData(body); }).catch((reason: unknown) => setError(reason instanceof Error ? reason.message : 'Riwayat tidak dapat dimuat'));
  }, [id]);
  if (error) return <div className="mx-auto max-w-3xl p-6"><div className="rounded-2xl border border-red-200 bg-red-50 p-5 text-red-800"><AlertCircle className="mb-2 h-5 w-5" />{error}</div></div>;
  if (!data) return <div className="flex min-h-[60vh] items-center justify-center"><Loader2 className="h-8 w-8 animate-spin text-[#0D47A1]" /></div>;
  return <main className="min-h-screen bg-[#F5F8FC] px-4 py-8 sm:px-6"><div className="mx-auto max-w-4xl space-y-6"><header className="rounded-3xl bg-gradient-to-br from-[#0D47A1] to-[#1976D2] p-6 text-white shadow-lg"><p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-100">Riwayat Rangkul</p><h1 className="mt-2 text-3xl font-black">Catatan kunjungan {data.lansia.nama}</h1><p className="mt-2 text-sm text-blue-100">Pantau perubahan kondisi dari laporan kunjungan yang sudah selesai.</p></header>{data.tren.perlu_perhatian && <div className="flex items-start gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-950"><ShieldAlert className="mt-0.5 h-6 w-6 shrink-0 text-amber-600" /><div><p className="font-bold">Perlu perhatian</p><p className="mt-1 text-sm">Rata-rata indikator menurun pada tiga kunjungan berturut-turut.</p></div></div>}<section className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-100 bg-white p-5"><HeartPulse className="h-5 w-5 text-[#0D47A1]" /><p className="mt-3 text-xs font-bold uppercase text-slate-400">Rata-rata terakhir</p><p className="mt-1 text-3xl font-black text-slate-900">{data.tren.rata_rata_terakhir ?? '-'}</p></div><div className="rounded-2xl border border-slate-100 bg-white p-5"><ArrowDownRight className="h-5 w-5 text-amber-600" /><p className="mt-3 text-xs font-bold uppercase text-slate-400">Perubahan</p><p className="mt-1 text-3xl font-black text-slate-900">{data.tren.perubahan ?? '-'}</p></div><div className="rounded-2xl border border-slate-100 bg-white p-5"><CalendarDays className="h-5 w-5 text-emerald-600" /><p className="mt-3 text-xs font-bold uppercase text-slate-400">Kunjungan selesai</p><p className="mt-1 text-3xl font-black text-slate-900">{data.kunjungan.length}</p></div></section><section className="space-y-4">{data.kunjungan.length === 0 && <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">Belum ada kunjungan selesai.</div>}{data.kunjungan.map((visit) => <article key={visit.task_id} className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{new Date(visit.waktu).toLocaleString('id-ID')}</p><div className="mt-4 grid gap-5 md:grid-cols-[160px_1fr]">{visit.foto_bukti_url ? <img src={visit.foto_bukti_url} alt="Bukti kunjungan" className="h-32 w-full rounded-xl object-cover" /> : <div className="flex h-32 items-center justify-center rounded-xl bg-slate-100 text-xs text-slate-400">Tidak ada foto</div>}<div><p className="font-bold text-slate-900">Catatan kondisi</p><p className="mt-1 text-sm text-slate-600">{visit.catatan_kondisi || 'Tidak ada catatan.'}</p>{visit.health_snapshot && <div className="mt-4 grid grid-cols-2 gap-2 text-xs text-slate-600 sm:grid-cols-5">{Object.entries({ energi: visit.health_snapshot.energi, mobilitas: visit.health_snapshot.mobilitas, mood: visit.health_snapshot.mood, nafsu_makan: visit.health_snapshot.nafsu_makan, kualitas_tidur: visit.health_snapshot.kualitas_tidur }).map(([label, value]) => <div key={label} className="rounded-lg bg-slate-50 p-2"><span className="block text-[10px] uppercase text-slate-400">{label.replace('_', ' ')}</span><b className="text-slate-900">{value}/5</b></div>)}</div>}{visit.health_snapshot?.cerita_hari_ini && <p className="mt-4 text-sm italic text-slate-600">“{visit.health_snapshot.cerita_hari_ini}”</p>}</div></div></article>)}</section><p className="text-center text-xs text-slate-500">{data.disclaimer}</p></div></main>;
}
