"use client";

import { motion } from "framer-motion";
import { BookHeart, CircleCheck, HeartPulse } from "lucide-react";

export default function HeroProductStack() {
  return <motion.article initial={{ opacity: 1, y: 20, rotate: 2 }} animate={{ opacity: 1, y: 0, rotate: 0 }} transition={{ duration: .65, delay: .2 }} className="rounded-[22px] border border-blue-100 bg-white/95 p-4 shadow-[0_20px_48px_rgba(13,71,161,.18)] backdrop-blur sm:p-5" aria-label="Contoh tampilan snapshot kunjungan"><div className="flex items-start justify-between gap-3 border-b border-blue-100 pb-3"><div><p className="text-[10px] font-extrabold tracking-[.13em] text-primary">CONTOH TAMPILAN</p><h2 className="mt-1 font-heading text-base font-bold text-foreground sm:text-lg">Ibu Ratna</h2><p className="mt-1 text-xs text-muted-foreground">Menemani Mengobrol · Selesai</p></div><span className="flex size-9 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-primary"><HeartPulse className="size-4" aria-hidden="true" /></span></div><div className="mt-3 grid grid-cols-3 gap-2">{[["Energi","Stabil"],["Mood","Tenang"],["Mobilitas","Tercatat"]].map(([label,value]) => <div key={label} className="rounded-xl bg-slate-50 p-2"><p className="text-[10px] text-muted-foreground">{label}</p><p className="mt-1 text-[11px] font-bold text-foreground">{value}</p></div>)}</div><div className="mt-3 rounded-xl border border-blue-100 bg-blue-50/70 p-3"><p className="flex items-center gap-1.5 text-[10px] font-extrabold tracking-[.1em] text-primary"><BookHeart className="size-3.5" aria-hidden="true" />CERITA HARI INI</p><p className="mt-1.5 text-xs leading-5 text-slate-700">Waktu mengobrol dan aktivitas ringan tersimpan untuk keluarga.</p></div><p className="mt-3 flex items-center gap-1.5 text-[11px] font-bold text-emerald-700"><CircleCheck className="size-3.5" aria-hidden="true" />Keluarga tetap memegang kendali</p></motion.article>;
}
